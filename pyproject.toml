[project]
name = "kobold-legacy"
version = "1.0.0"
description = "A discord bot game where players play the role of kobolds surviving in a harsh, low-tech environment."
readme = "README.md"
requires-python = ">=3.10"
license = "LICENSE"
dependencies = ["discord-py==1.7.3", "python-dotenv>=1.0.0"]

[dependency-groups]
linter = ["ruff>=0.9.8"]
